@component('controle.component.modal', [
    'id'                => $id,
    'method'            => $method,
    'title'             => $title,
    'width'             => $width,
    'stopButton'        => $stopButton,
])
@endcomponent
